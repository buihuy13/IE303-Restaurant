"use client";

export function useMerchantMessagesPage() {
  return {
    title: "Messages",
    emptyMessage: "No messages (mock).",
    emptyHint:
      "In production this would list customer conversations.",
  };
}
