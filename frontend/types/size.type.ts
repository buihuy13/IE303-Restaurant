export interface Size {
        id: string;
        name: string;
        createdAt?: string;
        updatedAt?: string;
}
export interface SizeData {
        name: string;
}
export interface SizePrice {
        sizeId: string;
        price: string;
}
